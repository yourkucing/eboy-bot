const Discord = require('discord.js');

module.exports.run = async(client, msg, args) => {
    //mel: Step2
    if (msg.channel.id == "847316136433025045") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "847345544317763615"), "")
        })
    }
    //res: Step3
    else if (msg.channel.id == "847317245058088990") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "847345578298834975"), "")
        })
    }
    //maryam: Step4
    else if (msg.channel.id == "847343405259554821") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848220554750197780"), "")
        })
    }
    //moon: Step5
    else if (msg.channel.id == "847856846168784896") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848220643522641994"), "")
        })
    }
    //nin: Step6
    else if (msg.channel.id == "847856934749077524") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848220674518810634"), "")
        })
    }
    //skylette: Step7
    else if (msg.channel.id == "847857007247228989") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848220703856656444"), "")
        })
    }
    //theo: Step8
    else if (msg.channel.id == "847857095935000656") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848220725856305163"), "")
        })
    }
    //hanna: Step9
    else if (msg.channel.id == "847857050465337374") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848220751769108492"), "")
        })
    }
    //bah: Step10
    else if (msg.channel.id == "847857153875640390") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848220776737931284"), "")
        })
    }
    //imogen: Step11
    else if (msg.channel.id == "847857199749005342") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848220801530462238"), "")
        })
    }
    //lissy: Step12
    else if (msg.channel.id == "847857301800353822") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848220828117499935"), "")
        })
    }
    //vinnie: Step13
    else if (msg.channel.id == "847857354481467393") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848220856831967264"), "")
        })
    }
    //squid: Step14
    else if (msg.channel.id == "847857481748840457") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848220878658469909"), "")
        })
    }
    //jack: Step15
    else if (msg.channel.id == "847857526023520316") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848220904718991380"), "")
        })
    }
    //dax: Step16
    else if (msg.channel.id == "847857638888439838") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848220944128016419"), "")
        })
    }
    //quinn: Step17
    else if (msg.channel.id == "847857690177044530") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848220967242039346"), "")
        })
    }
    //harley: Step18
    else if (msg.channel.id == "847857770992238613") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848220998935248936"), "")
        })
    }
    //ailyn: Step19
    else if (msg.channel.id == "847856810803462165") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848221021625385011"), "")
        })
    }
    //sahar: Step20
    else if (msg.channel.id == "847856900841668658") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848221049098338327"), "")
        })
    }
    //otter: Step21
    else if (msg.channel.id == "847857260084330595") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848221095172898816"), "")
        })
    }
    //maf: Step22
    else if (msg.channel.id == "847857453899055114") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848221117059432509"), "")
        })
    }
    //vi: Step23
    else if (msg.channel.id == "847857567820808224") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848221141701230612"), "")
        })
    }
    //finch: Step24
    else if (msg.channel.id == "847857848917688360") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848221167104819270"), "")
        })
    }
    //mer: final
    else if (msg.channel.id == "848005259758534676") {
        hooman = msg.author.id
        msg.channel.send(`Oh, let's go to another surprise! I hope you're having fun because I sure am. Let's gooooo! :heart:`).then(msg => {
            const birthdayboy = tempguild.members.cache.get(hooman)
            birthdayboy.roles.add(tempguild.roles.cache.find(x => x.id == "848222925348667462"), "")
        })
    }
}